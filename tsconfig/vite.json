{
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": "./dom.json",
	"compilerOptions": {
		"types": ["bun", "vite/client"], // import.meta.env + asset imports; restating replaces the array
		"paths": { "@/*": ["${configDir}/src/*", "${configDir}/*"] }
	}
}
