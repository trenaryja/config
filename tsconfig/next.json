{
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": "./dom.json",
	"compilerOptions": {
		"plugins": [{ "name": "next" }]
	},
	"include": ["${configDir}/**/*", "${configDir}/.next/types/**/*.ts", "${configDir}/.next/dev/types/**/*.ts"] // `**/*` skips dot-dirs
}
