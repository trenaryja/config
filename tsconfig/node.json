{
	"$schema": "https://json.schemastore.org/tsconfig",
	"extends": "./base.json",
	"compilerOptions": {
		"types": ["node"] // Node hosts (VS Code, Raycast) have no `Bun` global; replaces base's "bun"
	}
}
